<%= form_for @post do |f| %>
  <% if @post.errors.any? %>
    <h3><%= pluralize(@posts.errors.count, "erreurs") %> ont empêché la sauvegarde de cet article 😥</h3>
    <ul>
      <%= @posts.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :title %> <br>
  <%= f.text_field :title %> <br>
  <br>

  <%= f.label :content, "Rédige ton article ici" %> <br>
  <%= f.text_area :content %> <br>
  <br>

  <%= f.submit %>

<% end %>
